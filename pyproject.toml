[project]
name = "latency-monitor"
version = "0.1.0"
description = "Multi-target latency and network monitoring dashboard"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "Flask>=3.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
